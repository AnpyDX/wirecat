# Pcap SDK for Windows

## 1. Install Pcap

Check [Build on Windows - PcapPlusPlus](https://pcapplusplus.github.io/docs/install/vs).

## 2. Extract SDK

Download and extract SDK in this directory (`~/sdk`), `~/sdk/Include` is existed if have done correctly.
